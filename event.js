class Event {
  constructor(name) {
    this.name = name; // location must be added
    this.acceptedBy = [];
  } // participitans = [] must be added
}

module.exports = Event;

// class Review {
//     constructor(vet) {
//         this.vet = vet
//         this.reviewedBy = []
//     }
// }

// start  + end event must be defined
// participitans = [] must be added and pushed
